{
  "name": "twitter-clone",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm install && npm run build",
    "render-postbuild": "npm run build"
  },
  "engines": {
    "node": "16.x"
  }
}
